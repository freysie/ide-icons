import SwiftUI

// TODO: finish this:

//extension IDEIcon {
//  public struct Adjustment {
//    public var fontSize: CGFloat?
//    public var yOffset: CGFloat?
//  }
//
//  public static var adjustments: [IDEIconContent: Adjustment] = [
//    .text("@"):   Adjustment(fontSize: 1, yOffset: 1),
//    .text("{ }"): Adjustment(fontSize: -1, yOffset: 1),
//    .text("#"):   Adjustment(fontSize: 2.5),
//    .text("Ti"):  Adjustment(fontSize: 0.5),
//    .text("⨍"):   Adjustment(yOffset: 1),
//    .text("•"):   Adjustment(fontSize: 2.5, yOffset: 1),
//    .systemImage("puzzlepiece.fill"):                  Adjustment(fontSize: -1),
//    .systemImage("rectangle.connected.to.line.below"): Adjustment(fontSize: -0.1, yOffset: -0.3),
//    .systemImage("list.bullet"):                       Adjustment(yOffset: 1),
//  ]
//
////  public var adjustment: Adjustment {
////    switch content {
////
////    case .text("@"):
////      return Adjustment(fontSize: 1, yOffset: 1),
////
////    case .text("{ }"):
////      return Adjustment(fontSize: -1, yOffset: 1),
////
////    case .text("#"):
////      return Adjustment(fontSize: 2.5),
////
////    case .text("Ti"):
////      return Adjustment(fontSize: 0.5),
////
////    case .text("⨍"):
////      return Adjustment(yOffset: 1),
////
////    case .text("•"):
////      return Adjustment(fontSize: 2.5, yOffset: 1),
////
////    case .systemImage("puzzlepiece.fill"):
////      return Adjustment(fontSize: -1),
////
////    case .systemImage("rectangle.connected.to.line.below"):
////      return Adjustment(fontSize: -0.1, yOffset: -0.3),
////
////    case .systemImage("list.bullet"):
////      return Adjustment(yOffset: 1),
////
////    }
////  }
//}
